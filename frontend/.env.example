# Local development: backend runs on its own port.
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Production (Vercel): frontend and backend share one domain, and vercel.json
# routes /api/* to the backend service. Use the relative path.
# NEXT_PUBLIC_API_URL=/api
